// Fix the code to reverse.

(function (str) {
  var str1 = str.split(" ").reverse().join(" ");
  console.log(str1);
})("abcd");

// Use a space character as the delimiter instead of the quotation mark characters (“)
